server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://auth-db:5432/${AUTH_DB_NAME:platform_auth}
    username: ${AUTH_DB_USER:auth_user}
    password: ${AUTH_DB_PASSWORD:auth_pass}

  data:
    redis:
      host: redis
      port: 6379

management:
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: never
      probes:
        enabled: true
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true
